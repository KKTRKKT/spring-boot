version: "3.0"

services:
  postgres_db:
    image: postgres
    container_name: postgres_db
    ports:
      - 5555:5432
    environment:
      - POSTGRES_DB=springboot
      - POSTGRES_USER=kktrkkt
      - POSTGRES_PASSWORD=pass