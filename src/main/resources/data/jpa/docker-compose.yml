version: "3.0"

services: 
  jpa_db:
    image: postgres
    container_name: jpa_db
    ports:
      - 5556:5432
    environment:
      - POSTGRES_DB=springboot
      - POSTGRES_USER=kktrkkt
      - POSTGRES_PASSWORD=pass